fragment_downloaded_cb({"fragment": "Installing NodeJS \nIf you are following this tutorial on Endless OS you will need to install NodeJS since it s not included by default and there isn t a package for it. \nGo to the NodeJS download page and download the correct Linux Binaries package for your system. Most likely you will want the bit package. Create a local folder in your home directory and unzip the downloaded package there \nCheck if local bin is in your path echo PATH and if not add the following line to your bashrc file then close and reopen the terminal \n", "url": "tutorial/install.html#installing-nodejs"});