fragment_downloaded_cb({"url": "concepts/yaml_format.html#imports", "fragment": "Imports \nEach app must include its own app description but there is no need to write it from scratch. The framework provides pre defined apps descriptions or presets for different kinds of app experiences. The format includes the import tag to use these presets. See the example below \nIn this example the app includes its own complete copy of the news preset provided by the framework. The copy occurs while building the app. \n"});