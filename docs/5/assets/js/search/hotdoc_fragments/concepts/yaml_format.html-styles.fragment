fragment_downloaded_cb({"fragment": "Styles \nApp creators will likely need to style specific nodes and to avoid complex CSS selectors the easiest way to do it is by adding custom CSS classes. The format includes the styles key to add one or more CSS classes. See the example below \nIn this example a ContentGroup articles BEM style CSS class is added to a node. After that the CSS class can be used from the app s theme to style that specific node. \n", "url": "concepts/yaml_format.html#styles"});