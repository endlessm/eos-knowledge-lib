fragment_downloaded_cb({"url": "concepts/yaml_format.html#references", "fragment": "References \nSome modules need access to other modules properties. To do this the format includes the id and references keys see the example below \nIn this example a Layout.InfiniteScrolling module is added which needs access to the Selection.All module. To do this the selection module defines its identifier all articles using the id key. Then the scrolling module references the selection using the identifier. The lazy load reference name is what the scrolling module uses to access this reference. The list of available references names depends on each type of module. \n"});